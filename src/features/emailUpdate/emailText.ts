import { LNG_KEY } from '@src/i18n';

export const emailText = {
  placeholder: LNG_KEY.PLACEHOLDER_EMAIL,
  duplicateCheck: LNG_KEY.DUPLICATE_CHECK,
  confirm: LNG_KEY.CHECK,
  emailUpdateComplete: LNG_KEY.ALERT_03,
  emailDuplicateCheck: LNG_KEY.ALERT_02,
  checkFail: LNG_KEY.EMAIL_DUPLICATE_CHECK_FAIL,
  checkPass: LNG_KEY.EMAIL_DUPLICATE_CHECK_PASS,
};
